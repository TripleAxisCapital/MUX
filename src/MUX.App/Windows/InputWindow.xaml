<Window x:Class="MUX.App.Windows.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="410" Height="245" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="#F20F0F11" BorderBrush="#303036" BorderThickness="1" CornerRadius="20" Padding="26">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" FontSize="24" FontWeight="SemiBold" />
            <TextBlock x:Name="SubtitleText" Grid.Row="1" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,6,0,0" />
            <TextBox x:Name="ValueBox" Grid.Row="2" Style="{StaticResource MuxTextBox}" Margin="0,22,0,0" VerticalAlignment="Top" />
            <Grid Grid.Row="3" Margin="0,24,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <Button Content="Cancel" Style="{StaticResource MuxButton}" Click="Cancel_Click" />
                <Button Grid.Column="2" Content="Save" Style="{StaticResource MuxPrimaryButton}" Click="Save_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
