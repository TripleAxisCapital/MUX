<Window x:Class="MUX.App.Windows.CaptionResizePillWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="182" Height="44"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Opacity="0"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="PillButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="#F5F5F7" />
            <Setter Property="Background" Value="#242428" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="11,6" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonRoot"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonRoot" Property="Background" Value="#303036" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonRoot" Property="Background" Value="#3A3A41" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PillTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="#F5F5F7" />
            <Setter Property="Background" Value="#111114" />
            <Setter Property="BorderBrush" Value="#34343A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="CaretBrush" Value="#F5F5F7" />
            <Setter Property="SelectionBrush" Value="#555560" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="FieldRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="FieldRoot" Property="BorderBrush" Value="#707079" />
                                <Setter TargetName="FieldRoot" Property="Background" Value="#17171B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="Shell"
            Background="#F218181B"
            BorderBrush="#3A3A40"
            BorderThickness="1"
            CornerRadius="22"
            Padding="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="5" Opacity="0.42" Color="#000000" />
        </Border.Effect>

        <Grid ClipToBounds="True">
            <Grid x:Name="CollapsedPanel">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Style="{StaticResource PillButton}"
                        Width="72"
                        Content="Resize"
                        ToolTip="Set this window to an exact size"
                        Click="Expand_Click" />

                <Border Grid.Column="2"
                        Background="#111114"
                        BorderBrush="#2E2E33"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="10,5">
                    <TextBlock x:Name="CurrentSizeText"
                               Text="— × —"
                               Foreground="#A6A6AE"
                               FontSize="10"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                </Border>
            </Grid>

            <Grid x:Name="ExpandedPanel" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="72" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="72" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="58" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="32" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="WidthBox"
                         Style="{StaticResource PillTextBox}"
                         ToolTip="Window width in pixels"
                         PreviewKeyDown="DimensionBox_PreviewKeyDown" />
                <TextBlock Grid.Column="1" Text="×" Foreground="#777780" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <TextBox x:Name="HeightBox"
                         Grid.Column="2"
                         Style="{StaticResource PillTextBox}"
                         ToolTip="Window height in pixels"
                         PreviewKeyDown="DimensionBox_PreviewKeyDown" />
                <TextBlock Grid.Column="3" Text="px" Foreground="#777780" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <Button Grid.Column="5"
                        Style="{StaticResource PillButton}"
                        Content="Apply"
                        Click="Apply_Click" />
                <Button Grid.Column="7"
                        Style="{StaticResource PillButton}"
                        Padding="7,6"
                        Content="‹"
                        FontSize="16"
                        ToolTip="Back"
                        Click="Collapse_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
